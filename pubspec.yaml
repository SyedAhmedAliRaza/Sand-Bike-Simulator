name: sand_bike_sim
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.16.0
  flame_forge2d: ^0.19.2
  fast_noise: ^1.0.1
  # Updated plugins that support built‑in Kotlin (no KGP warning)
  sensors_plus: ^7.0.0   # latest
  device_info_plus: ^13.1.0
  vibration: ^3.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
