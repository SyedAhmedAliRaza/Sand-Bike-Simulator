import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Dummy test', (WidgetTester tester) async {
    // The default flutter create command generated a test for a 'MyApp' class,
    // which is from the default counter app. Since this is a custom game,
    // we replaced 'MyApp' with this dummy test to allow `flutter test` to pass.
    expect(true, isTrue);
  });
}
